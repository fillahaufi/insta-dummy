import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

const AddPost = () => {
	return (
		<View className="flex-1 items-center justify-center">
			<Text>Ini halaman AddPost</Text>
		</View>
	);
};

export default AddPost;
