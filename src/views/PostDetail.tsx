import React from "react";
import { View } from "react-native";
import { Text } from "react-native-paper";

const PostDetail = () => {
	return (
		<View className="flex-1 items-center justify-center">
			<Text>Ini halaman Post Detail</Text>
		</View>
	);
};

export default PostDetail;
